# Möbius Doc Drift (daily) — 2026-07-31

Drift found in 2 of 2 scannable repos. The registry (registry/portfolio.yaml) is a full build behind reality on Watchtower, and every `repo_local` path in the registry points at `~/Projects/` which does not exist. Mission-control also carries uncommitted dashboard changes (a new dependency) with no doc update. Five of seven portfolio repos could not be scanned: their registered local checkouts are absent.

## Coverage this run

- Scanned (present, git-tracked): `Mission-Control`, `Watchtower` (both under `/Users/snow/.openclaw/workspace/`).
- NOT scanned (registry `repo_local` path missing on disk): `preflight`, `gabay`, `sienna`, `theframeshift`, `workoutapp`. `~/Projects/` does not exist. Code/doc state for these five is unverified today. [EVIDENCE: `ls /Users/snow/Projects` -> No such file or directory]

## Repos with drift

### 1. registry/portfolio.yaml — stale against Watchtower reality [EVIDENCE, escalated]

- What changed in code: Watchtower shipped Build 3 (Gmail IMAP ingest + Telegram inbound + guarded outbound, v3 DB migration) across 8 commits today, ending in session-close `c172f17` (2026-07-31). Working tree clean.
- What didn't change in docs: portfolio.yaml slot 5 still reads `status: shipping`, `last_activity: 2026-07-19T08:31:55`, and `next_gate: "...before Build 2 is declared shipped"`. That gate is two builds behind: Build 2 shipped 2026-07-19, Build 3 shipped today.
- Suggested update: set `last_activity: 2026-07-31`, restate `next_gate` to the current Build 3 gate (real-provider validation of Gmail/Telegram + Docker deploy), and re-confirm `status`. This is a Möbius-owned field (`last_activity: Möbius updates`), held for the operator's go per advisory HITL.

### 2. registry `repo_local` paths — all wrong, breaks the sweep itself [EVIDENCE, escalated]

- What changed: the actual checkouts live under `/Users/snow/.openclaw/workspace/` (`Mission-Control/`, `Watchtower/`). The registry points every project at `/Users/snow/Projects/<name>` (e.g. `mission-control -> /Users/snow/Projects/missioncontrol`, `watchtower -> /Users/snow/Projects/watchtower`).
- Why it matters: this is the field the daily sweep resolves to find repos. Because it is wrong, five of seven repos are unscannable and this check is running blind on them.
- Suggested update: correct `repo_local` in portfolio.yaml and projects.yaml to the real workspace paths, or confirm whether the other five repos exist anywhere on this host. [INFERRED: they may simply not be checked out on this machine; needs operator confirmation, not a guess.]

### 3. Mission-Control — uncommitted dashboard drift [EVIDENCE, escalated target]

- What changed in code (working tree, uncommitted): `dashboard/package.json` adds dependency `croner ^10.0.1`; `dashboard/components/AgentRoom.tsx` (+12), `dashboard/lib/fs.ts` (+18) modified; `package-lock.json` updated.
- What didn't change in docs: no ROADMAP.md or DECISIONS.md entry, and the change is not committed. A new runtime dependency and a behavior change sitting in the working tree with no session-close is drift-in-progress on the escalated target.
- Suggested update: either commit with the ProjectOS session-close ritual (ROADMAP status + ADR for the `croner` dependency, docs and code in one commit) or stash/revert. Route to Helix as a "sync + commit dashboard" task only after operator go.
- Also uncommitted, untracked: `reports/mobius/2026-07-30.md`, `reports/torus/2026-07-30.md`, `reports/torus/2026-07-31.md`, `reports/prism/`. Advisory artifacts, low priority, but they are unpersisted crew output.

## Clean (checked, no drift)

- Mission-Control commit `ffc4731` (today): edited `agents/_master_brief.md`, `agents/cardioid/OVERLAY.md`, `agents/mobius/OVERLAY.md` WITH a matching `DECISIONS.md` ADR `[2026-07-31]` in the same commit. Complies with the hard rule (agents/ change requires same-commit ADR). No drift.
- Watchtower Build 3: code and docs moved together. `ROADMAP.md`, `PROJECT_OS.md`, `PARKING_LOT.md`, `README.md` all updated across the same session; clean working tree. No in-repo doc drift.

## Convention flag (not today's drift)

- Watchtower has no `DECISIONS.md`. Its ADRs live in `PROJECT_OS.md`. The house ProjectOS convention names four OS files including `DECISIONS.md`. [INFERRED] this may be deliberate for a single-repo, never-push project, but it deviates from the standard the master brief enforces elsewhere. Flag for the operator; not a Möbius fix.

## Recommendations (max 3)

1. Correct `repo_local` across the registry (or confirm the five missing repos are simply not on this host). Cost: ~15 min. Why: the daily sweep is blind on 5/7 repos until this is fixed. Highest priority.
2. Update portfolio.yaml slot 5 (Watchtower) to Build 3 reality. Cost: ~10 min, Möbius-owned field, needs operator go. Why: portfolio state is materially wrong on an active infra dependency.
3. Resolve the uncommitted Mission-Control dashboard change (commit with ADR, or revert). Cost: Helix task after approval. Why: escalated-target drift-in-progress, new dependency undocumented.

-- Möbius
