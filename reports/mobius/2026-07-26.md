# Möbius Doc Drift (daily) - 2026-07-26

Drift found in one repo: mission-control, the escalated target. Today's uncommitted working tree changes touch the two highest-risk path classes (agents/, schedule/) with no ADR in DECISIONS.md and no ROADMAP.md update. The other six portfolio repos could not be scanned this run: no local checkout exists at their registered paths (evidence below), so their same-day drift status is unknown, not clean.

## Repos with drift

### mission-control (escalated)

Evidence: `git status` on `/Users/snow/.openclaw/workspace/Mission-Control`, branch main, HEAD `1e5a98f` (2026-07-25T19:45). All changes below are uncommitted working tree edits with file mtimes of 2026-07-26T01:41-01:42 (today).

What changed in code/config today:
- `agents/_master_brief.md`: HITL channel switched from Telegram to Discord (two lines), and a new "MISSION-CONTROL AS A SPECIAL TARGET" section added (the ADR-per-change hard rule, Möbius escalation rule, and the two-HITL Helix rule).
- `schedule/scheduler.yaml`: every crew trigger flipped `enabled: false` to `enabled: true`. The whole scheduler is now live (Torus daily/weekly/monthly/quarterly, Möbius daily/weekly/monthly, Sphere morning brief, and the rest).
- `agents/sphere/OVERLAY.md` (24 lines): Telegram to Discord migration, plus a new "Discord channels" map (nine channel IDs) and routing rules.
- `agents/torus/OVERLAY.md`, `agents/helix/OVERLAY.md`, `agents/icosa/OVERLAY.md`, `agents/mobius/OVERLAY.md`: Telegram to Discord HITL wording, per-agent output-channel lines.
- `queues/decisions.jsonl`: two new Torus routing entries (dq-20260726-001 decided go; dq-20260726-002 pending).

What did not change in docs:
- `DECISIONS.md`: clean, last committed 2026-07-24. No ADR for the channel migration, the scheduler activation, or the special-target rules.
- `ROADMAP.md`: clean, last committed 2026-07-24. No phase/status entry for "scheduler activated" or "HITL moved to Discord."

Why this is drift, not noise: the master brief's own hard rule (added in this same uncommitted change) states that any agent-authored change to agents/, registry/, or schedule/ requires an ADR in DECISIONS.md in the same commit, "No ADR, no commit." The change that introduces that rule currently has no ADR behind it, and it sits alongside a full scheduler activation and a crew-wide channel migration. If this is committed as-is, it commits a rule violation on the first application of the rule.

Suggested doc update (Helix or operator, not Möbius):
- Add one ADR to `DECISIONS.md` dated 2026-07-26 covering three decisions: (1) HITL channel moved from Telegram to Discord with the nine-channel map, rationale being Watchtower not yet live; (2) scheduler triggers activated crew-wide; (3) mission-control special-target hard rules adopted. Cite each in the same commit as the code change.
- Add a `ROADMAP.md` line marking the scheduler as activated (status vocab: this is a state change, not a new phase).
- Commit code and docs together. Because the change is under agents/ and schedule/, the master brief now routes it through the two-HITL Helix flow; hold for operator go before applying.

## Coverage gap (not clean, unscanned)

`/Users/snow/Projects` does not exist. None of the registered local checkouts resolve: pre-flight, pinai, sienna, watchtower, frameshift_v2, missioncontrol are all missing. The six sibling repos (preflight, workoutapp, gabay, sienna, watchtower, theframeshift) exist only as GitHub remotes and were not cloned this run. Same-day code/doc drift for them is unverified. If daily coverage of remotes is wanted, the sweep needs read-only clones wired in, or the operator restores the local checkouts. Flagging for Sphere.

-- Möbius
