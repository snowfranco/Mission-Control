# Möbius Doc Drift, 2026-07-30

Drift found in Mission Control: today's working-tree code and operating-state changes have no matching ADR or ROADMAP update. All six registered source repos were scanned from fresh read-only clones; none has a commit dated 2026-07-30, so there is no same-day code-ahead-of-docs finding in those six repos. Three older code/doc inconsistencies remain open in Preflight, Watchtower, and The Frameshift.

## Repos with drift

### mission-control, escalated

**Evidence:** `/Users/snow/.openclaw/workspace/Mission-Control`, branch `main`, HEAD `75863eb` (2026-07-29). The 2026-07-30 working tree changes are uncommitted: `dashboard/components/AgentRoom.tsx`, `dashboard/lib/fs.ts`, `dashboard/package.json`, `dashboard/package-lock.json`, `agents/_master_brief.md`, `queues/decisions.jsonl`, and `queues/handoffs.jsonl` (`git status`, `git diff`).

- **Code/config changed:** dashboard agent timestamps now format in Toronto time; scheduled-agent cards now calculate the next cron run using `croner`; `croner` was added as a dependency. The master brief also gained a Discord agent-name formatting rule, and today's Torus/Prism queue records were appended.
- **Canonical docs not updated:** `DECISIONS.md`, `ROADMAP.md`, `PROJECT_OS.md`, and `PARKING_LOT.md` have no 2026-07-30 change. The last committed updates to these files are 2026-07-24 or earlier (`git log`). The code change is therefore uncommitted and the operating decision behind it is undocumented.
- **Suggested doc update:** add an ADR covering the dashboard timezone/next-run derivation and the Discord naming rule, then commit the dashboard, dependency lockfile, master brief, and queue changes together. Add a ROADMAP note only if this changes the Phase 2 shipped scope. This is a recommendation, not an authorization to apply it.

### Preflight, standing drift

**Evidence:** `PROJECT_OS.md` describes `web/` as both a real Phase 6 front end and a "README-only placeholder" (`PROJECT_OS.md`, lines 32-33). The repository contains `web/src/*`, `web/package.json`, and the Phase 6 commit `c86ac7b` records the shipped web app; `ROADMAP.md` marks Phase 6 shipped and Phase 7 next.

**Suggested doc update:** remove the stale placeholder sentence and keep the architecture description aligned with the shipped web implementation.

### Watchtower, standing drift

**Evidence:** `PROJECT_OS.md` and `AGENTS.md` say "Never push this repo to GitHub" and "Local commits only". The scanned repository nevertheless has `origin` set to `https://github.com/snowfranco/Watchtower.git` and `origin/main` at `c4a93eb` (`git remote`, `git log`). The Phase 1 registry review already flagged this contradiction, and it remains unresolved.

**Suggested doc update:** operator decides whether the no-push rule still applies. Then update the rule or remove the remote, with the decision recorded in the repo ADR log. Möbius does not choose between those options.

### The Frameshift, standing drift

**Evidence:** `ROADMAP.md` says it was last updated 2026-06-13, but commits `a704ecf` and `2eea9ba` on 2026-07-14 changed reliability behavior and the LaunchAgent path. The plist points to `/Users/snow/Projects/frameshift_v2`, while `docs/runbook.html`, `docs/context.md`, and code error messages still reference `~/frameshift_v2` (`git show`, repository grep).

**Suggested doc update:** update the roadmap date and post-2026-06-13 history, and normalize operational paths to the actual `Projects/frameshift_v2` location.

## Scanned with no new drift

Gabay, workoutapp, Sienna, and Watchtower had no 2026-07-30 commits in their GitHub histories. TheFrameshift and Preflight also had no 2026-07-30 commits, but their standing inconsistencies are listed above. Workoutapp's stale `replit.md` is explicitly superseded and parked in `PROJECT_OS.md` and `PARKING_LOT.md`, so it is not a newly discovered divergence.

No code or docs were fixed. Severe or current Mission Control drift should be routed by Sphere - Orchestrator to Helix - Builder only after the operator's approval.

-- Möbius
