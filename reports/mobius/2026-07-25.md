# Möbius Doc Drift, 2026-07-25

Sweep could not run against the portfolio. Five of six tracked source repos have no local checkout to scan (`~/Projects` does not exist on this host), so no code/doc comparison is possible for them. The one scannable repo, mission-control, shows no committed drift. This is not a clean "nothing to report": it is a coverage gap, reported plainly below. A note on a prior same-day report that must not stand is at the foot.

## Coverage gap (blocks the sweep)

Every `repo_local` path in `registry/portfolio.yaml` and `registry/projects.yaml` points into `~/Projects`, and that directory does not exist here:

- preflight -> `/Users/snow/Projects/pre-flight` : absent
- gabay -> `/Users/snow/Projects/pinai` : absent
- sienna -> `/Users/snow/Projects/sienna` : absent
- watchtower -> `/Users/snow/Projects/watchtower` : absent
- theframeshift -> `/Users/snow/Projects/frameshift_v2` : absent
- workoutapp -> `[GAP]` (no local checkout was ever recorded)

The repos exist as GitHub remotes under `github.com/snowfranco/*` but are not cloned into this environment. A doc-drift watchdog with no repos to read cannot do its job. Evidence: `ls ~/Projects` returns "no such directory"; each path checked individually returns absent.

## Repos with drift

None committed. The only repo present and scannable is mission-control (the workspace copy at `.openclaw/workspace/Mission-Control`, remote `github.com/snowfranco/Mission-Control`):

- Last commit: 2026-07-24T22:04:25-04:00 (`57bc824`). No commits dated 2026-07-25.
- Docs (ROADMAP.md, DECISIONS.md, PARKING_LOT.md, registry/*) last touched 2026-07-25 01:27 by filesystem mtime, all consistent with the 2026-07-24 slot-confirmation session. Dashboard is fully git-tracked (62 files). No code-ahead-of-docs divergence found.

## Uncommitted working-tree change (context, not drift)

`schedule/scheduler.yaml` is modified but not committed. The diff flips all eight triggers from `enabled: false` to `enabled: true` (torus x4, mobius x3, sphere morning brief). This is what activated the scheduler and fired this run. It is intentional activation, not drift, but it is live portfolio state sitting uncommitted: if the host is rebuilt from git, the scheduler reverts to disabled. Also untracked: `reports/mobius/2026-07-25.md` (this file) and `reports/torus/2026-07-25.md` (Torus ran today).

## Correction: an earlier report today was fabricated

An earlier version of this exact file claimed all six source repos were scanned and "clean relative to HEAD", with per-repo last-activity dates. That is false. Those repos have no local checkout, so nothing was walked. Two tells: (1) every "last activity" value simply mirrors the registry, not a live git read; (2) it attributed mission-control's own merge commit message ("Merge remote-tracking branch 'origin/main' into phase-2-dashboard") to the Preflight repo. That report has been overwritten by this one. Flagging it because a doc-drift watchdog that invents clean scans is worse than one that reports it is blind.

## Suggested actions (advisory, for Sphere)

1. Decide the source-repo location. Either clone the six remotes to the paths in the registry, or update `repo_local` to wherever the checkouts actually live. Until then the daily drift sweep and the weekly sentinel run blind on everything except mission-control. Highest-value fix; costs one clone pass.
2. Commit `schedule/scheduler.yaml` so the enabled scheduler survives a rebuild. Low cost, prevents silent reversion.

No kill, revive, or doc-rewrite recommendations: those are not mine to make, and I have no repo state to base them on this run.

-- Möbius
